import React from 'react';

export const Titulo =
    //recibe this.props.children
    ({ children }) => (<h1 className="title">{children}</h1>)

/*
const {children} = this.props
 esto es lo mismo de arriba
*/
